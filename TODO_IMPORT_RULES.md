# Desktop Todo 批量导入表格规范

版本：1.0  
用途：指导 GPT 将任意来源的信息整理为可直接粘贴到 Desktop Todo 的表格。

## 一、输出格式

必须输出制表符分隔表格（TSV）。第一行为表头，每一项任务占一行。

固定列顺序：

```text
任务	截止日期	状态	优先级	任务内容
```

不要在表格前后添加解释、标题、序号、Markdown 代码围栏或总结。需要解释时，应在用户确认后另行输出，不能混入待导入表格。

## 二、字段规则

### 1. 任务

- 必填。
- 使用简短、明确、可执行的任务名称。
- 建议使用“动词 + 对象”，例如“提交签证材料”“预约宿舍”。
- 不要把截止日期、状态、优先级重复写进任务名称。
- 可以保留有助于识别任务的 Emoji，但不能只使用 Emoji。
- 同一任务不要拆成多个措辞相近的重复项。

### 2. 截止日期

- 可选；无法从来源中确定时留空，不得猜测。
- 统一格式为 `YYYY-MM-DD`，例如 `2026-09-21`。
- 如果来源使用“今天、明天、下周五”等相对日期，必须根据用户当前时区和当前日期换算为绝对日期。
- 只有月份、没有具体日期时留空，并在“任务内容”中注明原始时间信息。
- 如果来源同时包含开始日期、结束日期和截止日期，优先使用明确标注的截止日期；没有截止日期时使用结束日期。
- 日期必须真实存在，例如 `2026-02-30` 无效。

### 3. 状态

- 必填，只允许以下三个值：
  - `待办`
  - `进行中`
  - `已完成`
- 映射规则：
  - 未开始、未处理、尚未执行、计划中、待处理、Pending、Todo → `待办`
  - 正在做、处理中、执行中、已启动、Doing、In Progress → `进行中`
  - 已完成、已处理、已提交、已办结、Done、Completed → `已完成`
- 来源没有状态时，默认填写 `待办`。
- 不要根据截止日期是否已过自动推断为已完成。

### 4. 优先级

- 必填，只允许以下三个值：
  - `高`
  - `中`
  - `低`
- 来源有明确优先级时按原意映射。
- 星级映射：
  - 4～5 星 → `高`
  - 2～3 星 → `中`
  - 1 星 → `低`
- 来源没有优先级时默认填写 `中`。
- 可根据明确语义映射：
  - 紧急、必须、阻塞其他事项、临近硬性截止 → `高`
  - 一般、正常计划 → `中`
  - 可选、有空再做、不影响主流程 → `低`
- 不要只因为日期较早就自动提高优先级，除非来源明确表达紧迫性。

### 5. 任务内容

- 可选；没有补充信息时留空。
- 保存完成任务所需的关键说明，例如材料、金额、地址、联系人、前置条件或完成标准。
- 使用简洁完整的句子，避免复制大段原文。
- 来源中不确定、冲突或缺少的信息可以写在此列，例如“原文只注明 9 月，具体日期待确认”。
- 单元格内不要换行，多个要点用中文分号 `；` 分隔。
- 不得包含密码、验证码、银行卡完整号码、身份证件号码等敏感信息。

## 三、任务识别规则

GPT 应将以下内容识别为任务：

- 明确要求某人执行的行动。
- 有截止时间、负责人或完成条件的事项。
- 用户做出的承诺或需要跟进的决定。
- 文档中的未完成步骤、检查项和准备事项。

以下内容默认不生成任务：

- 纯背景信息、观点和解释。
- 已取消或明确不再执行的事项。
- 没有行动要求的日期、人物或地点。
- 与现有任务完全相同的重复描述。

一段内容包含多个可以独立完成的行动时，应拆成多条任务；多个动作必须一起完成且共享同一截止日期时，可以合并为一条任务，并在“任务内容”中列出步骤。

## 四、去重规则

Desktop Todo 使用“标准化任务名称 + 截止日期”检测重复项。GPT 在生成表格前也应先去重：

- 任务名称相同且截止日期相同：保留一条。
- 名称措辞不同但行动目标明显相同：合并为一条，保留信息更完整的任务内容。
- 同名但截止日期不同：视为不同任务。
- 周期性任务或不同阶段任务：名称中明确阶段或周期，避免被误判为重复。

示例：

- “预约宿舍”和“完成宿舍预约”，日期相同 → 合并。
- “提交第一版报告”和“提交最终报告” → 保留两条。

## 五、不确定信息处理

- 不得编造任务、日期、状态、优先级或任务说明。
- 能合理确定任务但无法确定日期时，日期留空。
- 存在两个互相冲突的日期时，日期留空，并在任务内容中注明两个候选日期。
- 无法判断一段内容是否需要执行时，不直接生成任务；先向用户确认。
- 如果输入内容明显不完整，应先列出需要确认的问题，再生成最终导入表格。

## 六、标准输出示例

以下内容使用制表符分隔：

```text
任务	截止日期	状态	优先级	任务内容
预约宿舍	2026-04-21	已完成	高	确认房型、租期和付款方式
提交签证材料	2026-09-09	进行中	高	预约 VFS；核对 CAS、护照和资金证明
完成线上注册	2026-08-20	待办	高	支付学费并确认注册成功
整理抵英行李清单		待办	中	原文未提供具体截止日期
```

## 七、供 GPT 使用的标准提示词

```text
请根据我提供的信息提取所有可执行的待办事项，并严格按照 Desktop Todo 批量导入规范输出。

要求：
1. 只输出制表符分隔的 TSV 表格，不使用 Markdown 代码围栏，不添加解释。
2. 固定列顺序为：任务、截止日期、状态、优先级、任务内容。
3. 日期统一为 YYYY-MM-DD；无法确定时留空，不得猜测。
4. 状态只能是待办、进行中、已完成；没有状态时使用待办。
5. 优先级只能是高、中、低；没有依据时使用中。
6. 合并重复任务；同名且截止日期相同的任务只保留一条。
7. 任务名称应简洁、可执行；补充信息放在任务内容中。
8. 不要输出任何密码、验证码或完整证件号码等敏感信息。

待整理的原始信息如下：
[在这里粘贴聊天、邮件、文档、会议记录或其他内容]
```

## 八、GPT 输出前检查清单

- 是否包含且只包含五个固定列？
- 每一行是否都有任务名称？
- 日期是否全部为有效的 `YYYY-MM-DD` 或空白？
- 状态是否只使用待办、进行中、已完成？
- 优先级是否只使用高、中、低？
- 是否删除了重复任务？
- 是否把说明放进任务内容，而不是塞进任务名称？
- 是否避免猜测缺失信息？
- 是否移除了敏感信息？
- 是否可以直接整表复制并粘贴到 Desktop Todo？
