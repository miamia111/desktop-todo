# Desktop Todo 皮肤制作说明与 UI 设计规则

> 日历模块与贴纸扩展规范已纳入本文。新皮肤必须同时覆盖待办、时钟、闹钟、日历和设置窗口。

## 0. 使用方式与快速开始

Desktop Todo 支持两种皮肤接入方式。开始制作前必须先确定目标类型，不得混用两套流程。

### 0.1 本地扩展皮肤（推荐）

本地扩展皮肤无需修改应用源码或重新打包。每个皮肤使用皮肤资源目录下的一个独立子目录，例如 `themes/my-theme/`。实际资源目录应通过设置中的“打开皮肤文件夹”进入，不要硬编码某台电脑的用户目录；`.integration-test/*/themes` 仅属于测试配置。

最小目录结构：

```text
themes/my-theme/
├─ theme.json
└─ theme.css
```

最小 `theme.json`：

```json
{
  "id": "my-theme",
  "name": "我的皮肤",
  "description": "本地扩展皮肤",
  "previewColor": "#5577aa"
}
```

`theme.css` 必须使用与 ID 一致的作用域：

```css
body.theme-my-theme,
.theme-my-theme {
  --background: #f7f4ee;
  --foreground: #252525;
}
```

图片、字体和其他静态素材可放在皮肤目录内，并在 CSS 中使用相对路径，例如 `url("./assets/background.png")`。不要写绝对路径、`file:` 地址、远程 URL 或手工拼接的 `theme-asset:` 地址；应用会把合法相对路径转换为安全资源地址。

保存 `theme.json`、`theme.css` 或同目录素材后，在设置中点击“重新加载皮肤”即可生效，无需重新打包应用。

### 0.2 内置皮肤

只有当皮肤需要随应用发布、参与默认注册表或依赖源码级能力时，才应制作内置皮肤：

1. 在 `client/src/themes/registry.ts` 注册元数据。
2. 在 `client/src/index.css` 中使用 `.theme-{id}` 独立作用域实现样式。
3. 修改源码后运行检查和构建，并随应用重新打包。

普通用户皮肤、独立分发皮肤和快速迭代皮肤应优先使用本地扩展方式，不应为了换肤修改业务组件。

### 0.3 本地皮肤清单规则

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| `id` | 是 | 1–40 个小写字母、数字或连字符；必须与目录名一致 |
| `name` | 是 | 设置界面显示名称，简短且可读 |
| `description` | 是 | 描述体验或设计概念，不应只罗列颜色 |
| `previewColor` | 是 | 六位十六进制颜色，如 `#5577aa` |
| `supportsOpacity` | 否 | 只有真正支持材质浓度或窗口透明度时才设为 `true` |
| `stickers` | 否 | 日历贴纸数组，规则见“专项规范 B” |

## 专项规范 A：日历模块

日历窗口默认以最小基准尺寸 `560×440` 打开，可由用户拖拽放大。皮肤必须覆盖以下结构和状态：

| 选择器 | 用途 | 必须支持的状态 |
| --- | --- | --- |
| `.calendar-panel` | 日历内容根容器 | 普通、窗口缩放 |
| `.calendar-toolbar` | 月份标题、周/月切换和导航 | 普通、窄窗口 |
| `.calendar-view-switch` | 周/月分段选择 | default、hover、active、focus |
| `.calendar-nav` | 上一周期、今天、下一周期 | default、hover、active、focus |
| `.calendar-weekdays` | 星期标题 | 普通、次要文字可读 |
| `.calendar-day` | 日期单元格 | default、hover、selected、today、outside |
| `.calendar-day-header` | 日期与闹钟数量 | 最小宽度、普通宽度 |
| `.calendar-sticker-area` | 独立贴纸承载区 | 无贴纸、图片贴纸、Emoji 贴纸 |
| `.calendar-day-footer` | 独立待办数量行 | 无待办、有待办、文本截断 |
| `.calendar-entry-counts` | 待办文字与闹钟数字的紧凑指标组 | 窄单元格、超短单元格 |
| `.calendar-todo-count` | “X 项待办”文字 | 截断、窄单元格 |
| `.calendar-alarm-count` | 铃铛与闹钟数量 | 普通、窄单元格 |
| `.calendar-sticker` | 日期贴纸及贴纸编辑入口 | default、hover、focus |
| `.calendar-detail` | 选中日期详情 | 普通、无安排、有多项安排 |
| `.calendar-compose` | 新增待办与贴纸选择浮层 | default、focus、disabled |
| `.calendar-alarm-option` | 新增待办时同步创建闹钟 | 未勾选、已勾选、时间输入 |
| `.calendar-sticker-options` | 贴纸资源选择网格 | default、hover、active、滚动 |

日历设计必须满足：

- 今天、选中日期和非本月日期不能只靠同一种颜色区分。
- 日历打开时默认选中今天；单击日期后，`.calendar-day.selected` 的高亮外框必须立即移动到该日期。今天使用日期圆点表达，当前选择使用完整单元格外框表达，两种状态不得混为一体。
- 日期数字、待办数量和闹钟时间的优先级必须高于装饰贴纸。
- 日期格内使用“X 项待办”突出日程信息，闹钟使用铃铛加数字作为次要指标；详细标题与时间通过点击日期后的下方详情展示，不依赖小面积悬浮区。
- 底部日期详情必须分别显示“X 项待办 · X 项闹钟”，不得将两类内容合并为含义不明确的“X 项安排”。
- 贴纸不得遮挡日期、待办数量或闹钟时间。
- 最小月视图使用两层布局：顶部为日期与闹钟；底部在有贴纸时分为“待办文字列＋间距＋自适应贴纸列”，无贴纸时待办文字自动占满整行。周视图独立使用顶部信息、中部大贴纸、底部待办的三层布局。
- 贴纸尺寸由应用的 `.calendar-layout-v2` 响应式布局维护。当前月视图会根据日期格在约 `28–64px` 间缩放，周视图会在约 `72–160px` 间缩放，并继续受单元格可用高度限制。皮肤不得把 `.calendar-sticker-area`、`.calendar-sticker` 或其内部 `img` 锁定为 `24px`、`38px` 等固定尺寸。
- 皮肤只能修改日期格颜色、边框、阴影、字体与材质，不得重写月/周视图的网格轨道、贴纸容器尺寸、`max-width/max-height` 或信息区位置。尤其禁止用 `width/height/min-width/max-width: 24px !important` 一类规则覆盖应用的自适应契约。
- 日期格不放置新增按钮，新增待办统一通过底部按钮作用于当前高亮日期。
- 贴纸默认通过 `object-fit: contain` 或等效方式完整显示。主题可以根据明确的视觉方向使用圆形、圆角矩形或异形裁切；形状处理必须在月视图、周视图和贴纸选择器中保持一致，并且不能用来掩盖素材留白、主体过小或响应式尺寸失效。
- `.calendar-add` 和 `.calendar-sticker` 在键盘聚焦时必须可见，不能只依赖 hover。
- 周视图与月视图必须使用同一视觉语言；切换时不能造成控件丢失。
- 周视图具有独立的信息布局：贴纸在日期列下部居中，待办文字与闹钟数量在其下方居中，字号应高于月视图紧凑单元格。
- 透明皮肤下，日期格、详情条和弹层必须在复杂桌面背景上保持可读。
- 贴纸图片应使用透明背景；皮肤可调整旋转、阴影和滤镜，但不能阻断点击，也不能重新定义应用维护的响应式尺寸。源文件的有效图形必须充分利用画布，不能把过小主体放在大面积透明区域中。
- 应支持用户手动指定、删除贴纸以及恢复自动选择三种状态。
- 皮肤必须适配日期格宽度低于 `88px` 和 `64px` 的紧凑状态；可以缩小或隐藏指标图标，但必须保留两个数量及其语义区分。
- 当窗口高度不超过 `500px` 时必须启用最小高度布局：日期左上、闹钟数右上、待办文字左下、贴纸右下。四类信息不得共享同一位置或互相遮挡。
- 日历字体缩放以窗口宽、高中较小的比例为准，不应按窗口面积无限放大；超宽、超高窗口中正文建议保持在基准字号的 0.82–1.15 倍。

## 专项规范 B：贴纸资源扩展

本地皮肤可以在自己的资源文件夹中提供日历贴纸。推荐目录结构：

```text
themes/your-theme/
├─ theme.json
├─ theme.css
└─ stickers/
   ├─ rabbit.webp
   ├─ work.png
   └─ travel.svg
```

在 `theme.json` 中增加可选的 `stickers` 数组：

```json
{
  "id": "your-theme",
  "name": "皮肤名称",
  "description": "皮肤说明",
  "previewColor": "#5577aa",
  "stickers": [
    { "id": "rabbit", "name": "兔子", "source": "stickers/rabbit.webp" },
    { "id": "briefcase", "name": "工作", "source": "stickers/work.png" },
    { "id": "car", "name": "出行", "source": "stickers/travel.svg" }
  ]
}
```

字段规则：

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| `id` | 是 | 1–40 个小写字母、数字或连字符；同一皮肤内唯一 |
| `name` | 是 | 显示在贴纸选择器中的简短名称 |
| `source` | 是 | 相对皮肤根目录的图片路径，不允许绝对路径或远程 URL |

资源规则：

- 支持 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`、`.svg` 和 `.avif`。
- 推荐正方形透明图片，建议源尺寸为 `128×128` 或 `256×256`。文件像素尺寸达标不代表显示尺寸达标，还必须检查非透明有效图形的占比。
- 栅格贴纸在导出前必须按 alpha 通道紧裁有效图形，再放回正方形透明画布。有效图形外接框的最长边建议占画布的 `88%–94%`，四周各保留约 `3%–6%` 的安全留白；不得保留生成图片原有的大面积透明边缘。
- `128×128`、`256×256` 等画布像素尺寸只代表分辨率，不代表视觉主体大小一致。必须额外比较 alpha 有效边界、角色头部/身体占比和缩小后的视觉重量；两张同为 `256×256` 的图片，如果一张主体只占中央四分之一，应用中仍会显著更小。
- 单角色贴纸应让头部、身体或主要道具形成清晰的大轮廓。双角色、横向动作或长道具按整体组合的外接框计算最长边，不要求短边强行拉满，也不得为了保持正方形画布而把整个组合缩得过小。
- 贴纸必须在月视图最小显示尺寸（当前约 `28px`）、月视图放大尺寸和周视图大尺寸下都能辨认角色、主要表情和核心道具。只检查 `128×128`、`256×256` 原图或单一放大预览不算通过验收。
- 线稿贴纸缩小到约 `28px` 后，主轮廓应保持连续，眼睛、嘴巴等关键五官不能糊成一团或完全消失。同一套贴纸的角色比例、线宽、描边颜色、阴影和视觉重量应基本一致。
- 透明边缘必须检查 alpha 通道、透明四角和去底色残边；不得出现绿色、洋红色或其他色键毛边。自动去背后必须再次检查并按有效边界紧裁。
- 单张贴纸建议不超过 256 KB；动图必须克制，并支持静态首帧辨认。
- 文件名建议只使用小写英文、数字和连字符。
- 应至少提供 8 种贴纸，避免不同日程频繁重复。
- 建议保留语义 ID：`rabbit`、`star`、`sprout`、`car`、`books`、`noodles`、`briefcase`、`balloon`。使用这些 ID 时，自动匹配旅行、学习、餐饮、工作等日程可优先选择对应资源。
- 若皮肤没有提供 `stickers`，应用自动使用内置贴纸集合。
- 应用通过 `theme-asset://{theme-id}/{relative-path}` 安全加载资源；皮肤作者不应手写该协议地址。
- 修改贴纸文件或 `theme.json` 后，在设置中点击“重新加载皮肤”即可生效。

### 贴纸导出与验收流程

1. 生成或绘制贴纸时使用透明背景；若使用色键背景，先完成去背和去色边。
2. 通过 alpha 通道取得非透明有效图形的外接框，裁掉外接框之外的多余透明区域。
3. 按最长边缩放有效图形，使其占正方形画布的 `88%–94%`，居中放置并保留安全留白。不要使用只会缩小、不会放大的 `thumbnail` 类操作代替明确缩放。
4. 导出最终 PNG、WebP 或其他受支持格式后，验证透明角、文件大小以及 `theme.json` 中的相对路径。
5. 至少制作三张真实渲染接触表：月视图最小尺寸（约 `28px`）、月视图放大尺寸（约 `48–64px`）和周视图尺寸（至少 `72px`）。检查整套贴纸的主体大小和视觉重量是否一致，不能只看文件分辨率。
6. 在应用中分别检查最小窗口与放大窗口下的月视图、周视图和贴纸选择器；确认贴纸随日期格增大、角色可辨认、主题设定的轮廓处理一致，也没有遮挡日期与数量信息。
7. 使用开发者工具或临时检查样式确认 `.calendar-sticker-area`、`.calendar-sticker` 和内部 `img` 的计算尺寸随窗口变化。若外层变大而图片仍停留在 `24px` 左右，说明主题中仍存在固定尺寸或更高优先级的历史规则，不能通过继续追加 `!important` 解决。

贴纸属于皮肤资源而不是日程业务数据。日程仅保存贴纸 ID：切换皮肤后，如果新皮肤含有相同 ID，将自动使用新皮肤的对应资源；没有同 ID 时回退到内置资源或自动贴纸。

本文档用于指导开发者或 GPT 为 Desktop Todo 设计、实现和验收扩展皮肤。它是一份“兼容性契约”，不是固定视觉模板：必须保证功能、状态和可读性，但可以自由改变色彩、字体、材质、圆角、阴影、装饰和动效语言。

## 1. 设计目标

一套完整皮肤应同时覆盖待办、时钟、时区、闹钟、日历、贴纸选择器、设置菜单和窗口系统，并满足以下目标：

- 风格辨识度明确，各组件属于同一视觉世界。
- 主信息优先，桌面背景、装饰和材质不能妨碍阅读。
- 窗口在不同尺寸、桌面壁纸和窗口重叠状态下保持稳定。
- 所有业务功能、控件状态和点击区域保持可识别、可操作。
- 尽量通过主题令牌和 `.theme-{id}` 作用域实现，避免改动业务组件。
- 允许高自由度创作，但不能用“好看”交换可读性或功能完整性。

## 2. 皮肤运行架构

无论使用本地扩展还是内置注册，应用最终都会把主题类 `.theme-{id}` 应用到原生组件窗口或桌面环境。区别只在样式和元数据的来源：

| 类型 | 元数据来源 | CSS 来源 | 是否需要重新打包 |
| --- | --- | --- | --- |
| 本地扩展皮肤 | `themes/{id}/theme.json` | `themes/{id}/theme.css` | 否，设置中重新加载 |
| 内置皮肤 | `client/src/themes/registry.ts` | `client/src/index.css` | 是 |

新样式必须限制在自己的主题作用域内，不得无意修改其他皮肤。本地皮肤加载器禁止 `@import`、远程 URL、`data:` URL 和 `file:` URL；素材必须使用皮肤目录内的相对路径。

### 注册字段

以下 TypeScript 示例只适用于内置皮肤；本地扩展皮肤使用第 0 节的 `theme.json`。

```ts
export type Theme = 'minimal' | 'glass' | 'kawaii' | 'scifi' | 'your-theme';

{
  id: 'your-theme',
  name: '显示名称',
  description: '一句话描述体验，不只描述颜色',
  previewColor: '#RRGGBB',
  supportsOpacity: false,
}
```

只有真正支持材质浓度或窗口透明度的皮肤才应设置 `supportsOpacity: true`。

## 3. 规则强度

本文档使用三级约束：

- **必须**：功能和兼容性契约，不应破坏。
- **建议**：默认设计基线，可根据皮肤概念调整。
- **自由**：鼓励 GPT 发挥的部分。

### 必须保持

- 主题 CSS 只能在 `.theme-{id}` 下生效。
- 文本、输入、按钮、标签页、任务状态、闹钟状态必须可辨识。
- hover、focus、active、disabled、selected、completed、overdue 等状态必须有反馈。
- 不能隐藏窗口设置、置顶、最小化、关闭等核心操作。
- 不得依靠颜色作为状态的唯一线索；至少结合形状、图标、文字、明暗或描边之一。
- 不修改业务数据结构和行为，除非需求明确要求新增功能。
- 窗口重叠、透明度变化和尺寸变化时不得出现闪烁、反馈取样或文字消失。

### 可自由发挥

- 明亮或暗色基调、主色和强调色。
- 字体家族、字重组合和数字字体。
- 扁平、拟物、纸张、像素、复古、霓虹、液态玻璃等材质。
- 圆角大小、边框形式、阴影、纹理和装饰。
- 控件形状、状态表现和克制的主题动效。
- 信息密度，但仍需适配现有窗口最小尺寸。

## 4. 语义设计令牌

先定义令牌，再写组件覆盖。颜色值推荐使用 OKLCH，便于调整明度和透明度；也允许使用其他合法 CSS 颜色。

### 基础颜色

| 令牌 | 用途 | 规则 |
| --- | --- | --- |
| `--background` | 页面或内容底层 | 不应与主文字混淆 |
| `--foreground` | 主文字、关键数字 | 全主题最高阅读优先级 |
| `--card` | 卡片、任务项、信息块 | 与背景建立适度层级 |
| `--card-foreground` | 卡片主文字 | 通常接近 `--foreground` |
| `--popover` | 设置菜单、浮层 | 必须在复杂背景上稳定可读 |
| `--popover-foreground` | 浮层文字 | 与浮层背景形成清晰对比 |
| `--primary` | 主操作、选中状态 | 一套皮肤通常只设一个主要动作色 |
| `--primary-foreground` | 主操作上的文字 | 任何状态下均需清晰 |
| `--muted` | 次级容器、弱化背景 | 不抢主信息 |
| `--muted-foreground` | 辅助信息、日期、说明 | 仍须可读，不能等同于禁用态 |
| `--accent` | 强调、局部交互反馈 | 不应与危险色混淆 |
| `--destructive` | 删除、危险、严重逾期 | 应具有明确语义 |
| `--border` | 分隔线与普通边界 | 可为透明，但层级仍需通过其他方式体现 |
| `--input` | 输入控件表面 | 输入区域必须可识别 |
| `--ring` | 键盘焦点与选中描边 | focus 状态必须清楚 |

### 应用令牌

| 令牌 | 用途 |
| --- | --- |
| `--radius` | 主题基础圆角，可被组件局部调整 |
| `--window-frame-radius` | 原生窗口固定外圆角，由应用维护为 8px，皮肤不得覆盖 |
| `--win-bg` | 窗口主体材质 |
| `--win-border` | 窗口外边界或折射边缘 |
| `--win-titlebar` | 标题栏表面 |
| `--win-titlebar-text` | 标题和窗口图标 |
| `--win-shadow` | 窗口悬浮层级 |
| `--win-resize-handle` | 调整尺寸提示 |
| `--app-font` | 正文和控件字体 |
| `--app-font-display` | 大数字、标题或展示文字 |
| `--app-font-mono` | 等宽数字或科技风内容 |
| `--btn-primary-bg` | 主按钮表面 |
| `--btn-primary-text` | 主按钮文字 |
| `--task-complete-color` | 已完成状态 |
| `--clock-accent` | 时钟重点信息 |
| `--alarm-accent` | 闹钟重点信息 |
| `--glass-opacity` | 0 到 1 的材质浓度，仅透明皮肤使用 |

皮肤可以增加专属令牌，例如 `--paper-grain`、`--neon-glow` 或 `--liquid-rim`，但不要用专属令牌取代通用语义令牌。

## 5. 字体规范

字体规则以可读性和风格统一为目标，不强制某个固定字体。

### 建议层级

| 层级 | 典型内容 | 建议基线 |
| --- | --- | --- |
| 展示数字 | 当前时间、倒计时 | 2.4–4rem，允许随窗口缩放 |
| 窗口标题 | 待办事项、时钟与闹钟 | 0.84–1rem，600–700 字重 |
| 主内容 | 任务文本、时区名称 | 0.88–1rem，行高 1.4–1.6 |
| 控件文字 | 按钮、标签页、筛选 | 0.78–0.92rem，500–650 字重 |
| 辅助信息 | 日期、说明、数量 | 0.72–0.84rem，不得低于可读下限 |
| 微型标签 | 优先级、状态徽章 | 建议不低于 0.68rem |

### 字体要求

- 中文字体必须有可靠回退，不得只指定不含中文字形的字体。
- 数字需要稳定宽度时，可使用等宽字体或 `font-variant-numeric: tabular-nums`。
- 正文不建议使用过细字重；透明、图片或高纹理背景上建议至少 500。
- 辅助文字可以降低强调程度，但不应仅靠极低透明度弱化。
- 透明皮肤可使用文字阴影、局部遮罩或内容承载层保证阅读，不必固定为黑字或白字。
- 不要为了容纳内容无限缩小字体；优先换行、缩短标签或调整间距。

## 6. 颜色与对比规则

- 主文字与实际可见背景建议达到 WCAG AA：普通文字至少 4.5:1，大文字至少 3:1。
- 控件边界、图标和状态提示建议至少 3:1。
- 对透明皮肤，应以最复杂的桌面壁纸场景验收，而不是只对单色背景取样。
- 若背景明暗不可预测，应使用动态承载层、文字描边、阴影、局部模糊或较稳定的内容表面。
- `--muted-foreground` 是次要信息，不是不可读信息。
- 危险色、优先级色和完成色可以随主题变化，但语义必须在全皮肤内一致。
- 一套皮肤可以是单色设计；此时要通过明度、字重、形状和空间建立层级。

## 7. 组件设计规则

### 窗口与标题栏

涉及 `.native-widget`、`.app-window`、`.native-widget-titlebar`、`.window-titlebar`。

- `.native-widget` 的最外层圆角固定使用系统级 `8px`，不得由皮肤覆盖；较大的圆角、不规则轮廓和手绘边缘应放在标题栏、内容区、内层边框或伪元素中。
- 原生组件模式下 `html`、`body` 和 `#root` 必须保持透明，皮肤不得依赖 `body` 背景绘制窗口材质。
- 窗口主体、标题栏和内容区域应属于同一材质体系。
- 标题栏必须保留可拖动区域，交互控件不能被拖动层覆盖。
- 设置、置顶、最小化和关闭按钮的点击区域建议不小于 28×28px。
- 边框可以取消，但必须通过阴影、明暗、折射或材质差建立窗口边界。
- 装饰伪元素必须设置 `pointer-events: none`，不能阻挡交互。

### 待办任务

涉及 `.todo-list`、`.todo-item`、`.todo-text`、`.todo-checkbox`、`.todo-status-badge`、`.badge-priority`、`.todo-deadline`。

- 任务正文是视觉中心，优先级和状态应低于正文但容易扫描。
- 待办、进行中、已完成必须至少有两种视觉差异。
- 已完成文字可以弱化或划线，但仍需辨认。
- 逾期必须明显，不能只用非常接近普通文本的颜色。
- hover 不应引起布局跳动；拖动状态应明确显示当前对象和目标位置。
- 删除按钮默认可弱化，hover/focus 后必须清楚。

### 添加任务与普通按钮

涉及 `.app-btn-primary` 和普通操作按钮。

- 同一层级的“添加任务”和表单内“添加”应使用统一的主按钮语言。
- 主按钮必须包含 default、hover、active、focus 和 disabled 状态。
- 图标与文字间距保持一致，图标不能成为唯一标签。
- 主操作应明显，但不应使用多个互相竞争的主色。

### 输入与选择控件

涉及 `.app-input`、`.app-select`、日期输入和下拉选择。

- 输入区域必须与普通卡片区分。
- placeholder、已输入文本和 disabled 文本必须有不同层级。
- focus 状态使用 `--ring` 或等效视觉提示，不得只移除 outline。
- 控件高度、圆角、边框和字体应与按钮属于同一组件体系。

### 标签页、筛选和开关

涉及 `.win-tabs`、`.win-tab`、筛选按钮和设置开关。

- 选中状态必须同时通过颜色以外的方式表达，例如实心表面、滑块位置、描边或字重。
- 未选中项仍需可读，不应像 disabled。
- 标签切换时避免大幅度尺寸变化。

### 时钟、时区与闹钟

- 当前时间是最强信息层级，日期和星期为次级。
- 表盘、数字时钟和背景之间必须保持对比。
- 时区名称、时间和删除操作应形成清晰顺序。
- 闹钟开启、关闭、响铃中必须明显不同。
- 响铃动效可以更强，但应避免持续高频闪烁。

### 设置菜单

- 设置浮层必须高于内容，不能因透明度过低而融入桌面。
- 皮肤预览色仅用于快速识别，不代表皮肤只能使用该颜色。
- 透明度控件仅在皮肤确实支持时显示。
- 设置菜单也必须完成主题化，不能遗留默认组件视觉。

## 8. 透明与玻璃皮肤专项规则

- `--glass-opacity` 使用 0–1 数值，UI 可以显示为 0%–100%。
- 透明度控制的是材质底色浓度，不应同步降低文字和核心图标透明度。
- 0% 时也必须保证主要功能可操作、文字可阅读。
- 原生 Electron 窗口使用系统 Acrylic 时，避免在最外层再次使用 CSS `backdrop-filter`，以免窗口重叠时发生重复取样、闪烁或色彩跳变。
- 可在内部卡片、菜单或输入区使用局部材质，但嵌套模糊层应尽量少。
- 玻璃边缘可通过内外高光、反向暗边、渐变或折射纹理表现；装饰不得形成厚重实体边框。
- 在浅色、深色、高饱和和高纹理壁纸上分别测试。
- 两个透明窗口交叠并移动时，画面应稳定。

## 9. 动效规则

- 动效服务于状态反馈和风格表达，不应拖慢频繁操作。
- 通用交互建议 120–220ms；弹层或窗口可为 180–320ms。
- 优先动画 `opacity` 和 `transform`，谨慎动画布局尺寸和模糊半径。
- hover 不建议产生明显位移；active 可使用轻微缩放或下压。
- 支持 `prefers-reduced-motion`，重要状态不能仅靠动画传达。
- 科幻、可爱或游戏化皮肤可以有更强动效，但待办录入和勾选仍应迅速。

## 10. 响应式与窗口尺寸

- 当前原生窗口基准约为待办 380×560、时钟 360×540、日历 560×440，并允许缩放；日历默认即以最小尺寸打开。
- 在最小窗口下不得出现核心按钮被遮挡、标题栏控件溢出或内容无法滚动。
- 长任务文本必须换行，长时区名称应截断或弹性布局。
- 不要使用依赖固定屏幕坐标的装饰。
- 使用 `rem`、弹性布局和现有 `--widget-scale` 适配尺寸变化。
- 日历贴纸还会使用日期格的容器查询单位 `cqi` 自适应。皮肤应继承应用布局，不要用固定 `px`、行内尺寸或 `!important` 把贴纸恢复成单一大小。
- 必须同时测试“只增加宽度”“只增加高度”和“宽高同时增加”三种窗口变化；不能只在默认窗口或最大窗口验收。
- 图片文件尺寸、CSS 盒子尺寸和图片中视觉主体尺寸是三个独立指标，必须分别检查。盒子已经变大但主体看起来没有变大时，应先检查内部 `img` 的计算尺寸和素材有效边界。

### 10.1 CSS 级联与历史规则清理

- 同一组件只保留一组权威规则。修改贴纸、时钟或任务条目时，应直接更新或删除旧规则，不要连续追加 `v2`、`v3`、`final fix` 等覆盖块。
- 提交前搜索同一选择器及关键属性，例如 `calendar-sticker`、`width`、`height`、`max-width`、`border-radius` 和 `object-fit`，确认没有相互矛盾的历史声明。
- `!important` 只能用于确实需要越过应用内联样式的少数属性，不能作为解决级联冲突的默认办法。若需要更高优先级，优先缩小旧规则范围或删除旧规则。
- 当外层容器尺寸正确而内部图片仍很小时，应沿 DOM 顺序检查 `.calendar-sticker-area`、`.calendar-sticker`、内部 `img` 三层，不要只修改最外层。
- 本地皮肤重新加载后必须再次检查计算样式。文件已经复制、图片已经是 `256×256` 或 CSS 能通过解析，都不能代替实际渲染验收。

## 11. 实现流程

### 11.1 通用设计流程

1. 用一句话定义皮肤概念、情绪和材质，并明确是本地扩展还是内置皮肤。
2. 确定主文字、次要文字、背景、卡片、主操作和危险色。
3. 定义基础令牌与应用令牌。
4. 确定主题 ID、名称、说明、预览色和透明度支持。
5. 先完成窗口、标题栏、任务、按钮、输入和设置菜单。
6. 再覆盖时钟、时区、闹钟、日历、贴纸选择器、徽章、滚动条和装饰。
7. 补齐所有交互状态。
8. 在实际 Electron 窗口中测试尺寸、壁纸、窗口重叠和键盘操作。

### 11.2 本地扩展交付流程

1. 创建 `themes/{id}/theme.json` 与 `theme.css`，确保目录名、清单 ID 和 CSS 作用域一致。
2. 将图片、字体和贴纸放入该皮肤目录，并只使用相对路径引用。
3. 在设置中点击“重新加载皮肤”，检查皮肤是否因清单、CSS 或资源路径无效而被跳过。
4. 验证待办、时钟、闹钟、日历和设置窗口，不以“文件存在”代替实际应用内验收。
5. 搜索并删除重复选择器、旧版本修复块、固定尺寸残留和已经失效的 `!important`。
6. 若皮肤作为独立文件夹交付，最终清理色键源图、临时预览、未引用资源和旧版本素材。

### 11.3 内置皮肤交付流程

1. 在 `client/src/themes/registry.ts` 注册主题。
2. 在 `client/src/index.css` 中实现 `.theme-{id}` 样式。
3. 运行 `npm run check` 和 `npm run build`。
4. 在正式构建的 Electron 窗口中完成通用验收。

## 12. 验收清单

- [ ] 皮肤只影响自己的 `.theme-{id}` 作用域。
- [ ] 已明确皮肤属于本地扩展或内置注册，没有混用 `theme.json/theme.css` 与 `registry.ts/index.css` 流程。
- [ ] 本地皮肤的目录名、`theme.json` 中的 `id` 和 `.theme-{id}` CSS 作用域完全一致。
- [ ] 本地皮肤不包含 `@import`、绝对路径、远程 URL、`data:` URL 或 `file:` URL。
- [ ] 待办、时钟和日历三个窗口均完成主题化。
- [ ] 日历周/月视图、日期状态、贴纸选择器和新增待办浮层均完成主题化。
- [ ] 皮肤贴纸资源均可加载，名称可读，透明边缘和缩放效果正常。
- [ ] 每张栅格贴纸已经按 alpha 有效边界紧裁，不存在主体很小但透明画布很大的情况。
- [ ] 有效图形最长边约占画布 `88%–94%`，单角色、双角色和横向构图均保留合理安全边距且没有裁边。
- [ ] 已按月视图最小尺寸、月视图放大尺寸和周视图尺寸检查整套贴纸，角色、主要表情和核心道具仍可辨认，视觉重量基本一致。
- [ ] 已在最小窗口和放大窗口的月视图、周视图及贴纸选择器中检查贴纸，确认尺寸会随单元格变化；未通过固定容器尺寸掩盖源素材留白问题。
- [ ] 已分别检查贴纸承载区、按钮和内部图片的计算尺寸，不存在外层放大但内部图片仍被固定为约 `24px` 的情况。
- [ ] 贴纸的圆形、圆角矩形或异形轮廓属于明确的主题设计选择，并在月视图、周视图和选择器中表现一致。
- [ ] CSS 中没有重复的贴纸尺寸规则、旧版本覆盖块或互相冲突的 `width/height/max-width/max-height !important`。
- [ ] 去背贴纸没有色键残边，透明四角与 alpha 通道正常。
- [ ] 主文字、辅助文字、按钮文字均清晰。
- [ ] 添加任务按钮与表单操作视觉统一。
- [ ] 输入、选择、标签、开关和菜单没有遗留默认样式。
- [ ] hover、focus、active、disabled、selected 状态齐全。
- [ ] 待办、进行中、完成、逾期和响铃状态可区分。
- [ ] 设置、置顶、最小化和关闭功能可见可用。
- [ ] 最小窗口和放大窗口均无溢出。
- [ ] 透明皮肤在 0%、中间值和 100% 下均可读。
- [ ] 透明窗口重叠拖动时无明显闪烁或反馈取样。
- [ ] 不依赖远程图片才能正常显示。
- [ ] 本地皮肤已通过“重新加载皮肤”在实际应用中生效，交付目录中没有色键源图、临时预览或未引用旧素材。
- [ ] 类型检查和构建通过。

## 13. 可直接交给 GPT 的建议命令

```text
请为 Desktop Todo 设计并实现一套名为「{皮肤名称}」的扩展皮肤。

先完整阅读 client/src/themes/皮肤制作规范.md，并遵守其中的功能契约、语义令牌、组件状态和验收规则。视觉方向是：{描述风格、情绪、材质、时代或参考对象}。

接入方式：{本地扩展皮肤/内置皮肤，默认选择本地扩展皮肤}。

创作自由度：高。不要机械复制现有皮肤，也不要把参考对象照搬成品牌仿制品。你可以自由设计字体、配色、圆角、材质、阴影、装饰和动效，但必须保留 Desktop Todo 的全部功能和信息层级。

特别要求：
- 主文字风格：{例如高对比暖白、深墨黑、像素字体}
- 主色或禁用色：{可选}
- 材质：{例如纸张、金属、玻璃、无材质限制}
- 信息密度：{紧凑/舒适/宽松}
- 透明度支持：{需要/不需要}
- 必须避免：{可选}

请执行以下工作：
1. 若选择本地扩展，在皮肤资源目录创建 themes/{id}/theme.json、theme.css 和所需素材；不要修改 registry.ts 或 index.css。
2. 若明确选择内置皮肤，才在 client/src/themes/registry.ts 注册，并在 client/src/index.css 中实现 .theme-{id}。
3. 覆盖窗口、标题栏、待办、按钮、输入、标签页、时钟、时区、闹钟、日历、贴纸选择器和设置菜单。
4. 补齐 hover、focus、active、disabled、selected、completed、overdue 和 ringing 状态。
5. 不改变业务逻辑；如确需改变，先说明理由并保持改动最小。
6. 本地扩展必须在设置中重新加载并做应用内验收；内置皮肤必须运行类型检查和正式构建。
7. 最后按本文档第 12 节验收清单逐项检查并报告结果。
```

### 简短命令模板

```text
阅读 client/src/themes/皮肤制作规范.md，按照其中的皮肤契约，为 Desktop Todo 制作「{名称}」本地扩展皮肤。方向：{一句话描述}。在 themes/{id}/ 中创建 theme.json、theme.css 和本地素材，不修改内置注册或业务逻辑；覆盖全部组件和状态，保证可读性、窗口缩放与重叠稳定，并在完成后重新加载皮肤、执行应用内验收。
```

## 14. 不建议的实现方式

- 在业务组件中大量写主题判断或硬编码某套皮肤颜色。
- 使用无主题作用域的全局选择器覆盖其他皮肤。
- 只修改窗口背景和主按钮，遗漏设置、输入、状态和时钟。
- 通过降低整个窗口 `opacity` 实现透明度，这会同时使文字不可读。
- 在原生 Acrylic 窗口最外层叠加多个 `backdrop-filter`。
- 用超小字体、超低透明度或纯颜色差表达重要信息。
- 为装饰引入不稳定的远程资源或阻挡点击的伪元素。
- 为追求动效而持续动画模糊、阴影或大面积背景。

这份规则允许皮肤拥有完全不同的视觉语言，但要求每一套皮肤都像一个完整、可靠、可长期使用的 Desktop Todo 界面，而不是局部换色。
